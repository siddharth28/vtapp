<% if current_user.super_admin? %>
  <li><%= link_to 'Company Accounts', companies_path, class: "#{ state(companies_path) }" %></li>
  <li><%= link_to 'Create Company', new_company_path, class: "#{ state(new_company_path) }" %></li>
<% end %>

<% if current_user.account_owner? || current_user.account_admin? %>
  <li><%= link_to 'Users', users_path, class: "#{ state(users_path) }" %></li>
<% end %>

<% if !current_user.super_admin? %>
  <li class="dropdown mainmenu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Tracks<b class="caret"></b></a>
    <%= render 'shared/track_navigation_links' %>
  </li>
<% end %>

<li><%= link_to 'Account', edit_user_registration_path, class: "#{ state(edit_user_registration_path) }" %></li>
<li class='pull-right'><%= link_to 'Sign out', destroy_user_session_path, { method: :delete } %></li>
<li class='pull-right m-top user-detail'><%= "Hello, #{ current_user.name }" %></li>