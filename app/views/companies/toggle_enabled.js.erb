// FIXME_AB: Avoid using js in erb file. move to js file as a callback to ajax request
// FIXME : use dom_id helper here
$("#company_<%= @company.id %>").html("<%==j render partial: 'link_to_enabled', locals: { company: @company } %>");
