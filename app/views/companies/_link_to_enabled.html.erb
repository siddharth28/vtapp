<% val = company.enabled ? 'Disable' : 'Enable' %>
<%= link_to val, company_toggle_enabled_path(company), remote: true, id: "company_#{ company.id }", data: { confirm: "Are you sure you want to #{ val } the company?" } %>
